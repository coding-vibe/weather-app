import DateRangePicker from 'components/DateRangePicker';
import * as classes from './styles';

export default function HistoricalForecast() {
  return <DateRangePicker css={classes.dateRangePicker} />;
}
